'use strict';

var assert = require("assert");
var should = require("chai").should();

describe('Engine', () => {

  it('should be true', () => {
    var one = 1;
    one.should.be.equal(1);
  });

});
